# 第三幕：月面人生花园

## 本次范围与场景约定

按用户主参考图保留黑白月面、地球、右侧档案舱、单一主角和左侧零点小姐文案；花朵采用用户提供的年龄阶段示意。没有改动门的模型、开启方向或原有开门时间线。

- 玩家意图：带着第二幕登记资料，进入想回看的年龄。
- 主动作：沿原路线前进到门口；第三幕选择／种下一个年龄章节，再查看记录。
- 场景切换：门口冻结末帧，950ms 渐入全白，白屏至少550ms，约1150ms 显露月面。下一幕图片预先解码，失败不无限等待。低动态偏好使用更短的淡入淡出。
- 反馈：悬停仅亮环、人物转向；点击后人物沿地面走近、蹲下、播种、起身，完成后才保存种植。动作细节见 `garden-player-motion.md`。时间节点切换时短暂流星划过，不持续晃动整个背景。
- 人物与证据：复用档案室玩家精灵，背景不含人物。相近人生信号明确标记“待接入”，没有虚构真实人物、时代事实、匹配分数。
- 出口：可操作的年龄节点与记录面板，而非说明页。R 关闭当前面板／中止未完成种植并回到本画面，不删除资料。

## 入口

- `/?scene=archive` 或 `/?debug=archive`：进入第二幕。填写五问、观看人生审查、门完全打开后，按住 W／↑ 或前进按钮走到门口，自动过白屏进入第三幕。
- `/?scene=garden`：直接预览第三幕。已有保存则恢复，没有登记则明确提示缺失，不伪造用户年龄。
- `/?debug=garden`：测试入口，跳过前两幕，用标明为演示的资料新开第三幕。可加 `age` / `rewind` 参数。
- 原首页海边 → 月球回溯 → 穿梭 → 档案室入口保留。

## 第二幕数据交接

`createArchiveScene` 到达门口时发出 `life-backtest:archive-door-arrived`，携带 `detail.profile`。主入口把资料直接传入 `gardenScene.show(profile)`，不是从截图识别。

资料字段继续使用 `age / gender / family / status / rewind`。第二幕原始数据仍保存在会话键 `life-backtest.archive-profile`；第三幕以 `life-backtest.garden.v2` 在本机保存一份登记快照及探索进度（`planted` 为 `{ age, choiceId }[]`，另有 `reachedPresent`）。从档案室走进花园会新开一局，不接上一局三岔口；`/?scene=garden` 才续玩。不会自动上传。旧键 `life-backtest.garden.v1` 会在启动时清除。

现实年龄 `currentAge`、原始回溯答案 `target.raw`、确认的回溯年龄 `target.age`、正在查看的年龄 `selectedAge` 分开保存。例如24岁／毕业前，先保留原话，再由用户确认18岁；现实年龄仍为24。

明确的“18岁”直接定位。年份或“毕业前”不能凭空推算出生年份，因此仅追问目标年龄，不重复收集五项资料。精确的18岁、83岁会加入当前时间线。当前家庭／就业情况只是现实上下文，不能当成18岁时的事实。

## 花朵与节点

| 章节 | 花形 |
| --- | --- |
| 0–10 | 种子 |
| 10–20 | 含苞 |
| 20–30 | 待放 |
| 30–40 | 盛放 |
| 40–50 | 初落 |
| 50–60 | 渐落 |
| 60–70 | 结籽 |
| 70以后 | 延续（保留种籽形，节点可到100岁） |

边界属于下一章节，例如30岁属于30–40。页面使用四个固定种植位置，分别承载0–40岁和40岁以后两页章节，页按钮可切换；底部是这一章的具体年龄，左右方向键和屏幕箭头均可切换。晚于现实年龄的节点明确写作探索，不是未来预言。花形只是视觉隐喻，不是健康或人生价值判断。

悬停不改变存档；第一次点击走近并完成种植，重复点击走近、轻触后打开该年龄的记录面板。同章切换具体年龄只更新内容，不重复走路。种植与记录按章节／年龄保存，刷新不重复种植，不清空原始资料。

## 留给队友的接入点

打开已种植章节时，场景发出冒泡事件 `life-backtest:garden-node-request`。`detail` 包含：

- `profile`：第二幕五项原始资料。
- `currentAge`：现实年龄（可为空）。
- `target`：原话、类型、确认年龄、原始年份。
- `selectedAge` 和 `chapter`：当前查看年龄、章节索引。
- `mode`：`rewind` 或 `future-exploration`。
- `records`：当前年龄及以前由玩家主动留下的笔记。
- `planted`：已种下的 `{ age, choiceId }[]`。
- `reachedPresent`：是否已进入月面三岔口。

种到现年后出现四颗像素小行星。进入裂隙页时另发出 `life-backtest:garden-rift-open`，`detail.kind` 为 `backtrack` / `forward` / `foresight` / `end`。四页文案均为演示，页面标明「演示内容 · 不是真实匹配结果」。

这是前端请求上下文，不是已经接通模型的网络接口。本次没有实现视频内的检索／推演系统，也没有结果回填或真实人物联系功能。队友后续可监听此事件、调用服务，并在现有时间档案面板或裂隙页接入问题、选项、时代背景及来源。来源事实、推断和生成叙述必须分开标记。确认接口授权与必要字段后再上传，不将本机笔记默认公开。

## 素材来源

- `public/assets/garden/moon-garden-clean-v1.png`：根据用户主参考生成的干净月面底图，移除烧录文案、花朵与主角，保留地球和档案舱构图。
- `public/assets/garden/flower-reference-v1.png`：用户七阶段参考原图的副本；运行时 Canvas 取各朵花的窗口，排除原图文字和光环，交互光环由网页绘制。
- 玩家行走复用 `player-walk-sheet-v2.png`；播种使用同一角色参考生成的 `player-plant-sheet-v1.png`，动作出处见 `garden-player-motion.md`。

## 验证记录

自动用例覆盖：海边到档案室、五问与审查后开门、W前进到门、纯白帧、第三幕资料继承与目标补充、R保留资料、刷新恢复、悬停不种植、种植完成、年龄笔记、方向键、80岁节点、窄桌面及390px竖屏／低动态偏好。

画面证据在 `playtest/`，包括 `archive-garden-06-white.png`、`archive-garden-07-entered.png` 及 `garden-01-hover.png` 至 `garden-05-portrait.png`。浏览器复核主角只出现一次、场景和文案分层、花朵点击有明确结果。电脑横屏是本次主要演示布局；竖屏改为可滚动布局以保留触控可用性。

当前属于可玩的场景框架，不宣称平行人生模型已完成。构建仍有单包大于500KB的体积提示，不影响本次流程运行。

2026-09-03 验证结果：类型检查与构建通过，33个单元测试、5个浏览器流程测试通过。按游戏导演视觉复核，横屏分类为可交互的游戏场景框架；鼠标悬停、种植中、完成后、资料缺失和记录面板均已截图检查。音效调用与种植状态同源，未进行主观听感验收。
